[project]
name = "icberg-polaris-v1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.2.0",
    "faker>=40.15.0",
    "pandas>=3.0.2",
    "pyarrow>=24.0.0",
    "pyiceberg[pyarrow,s3fs]>=0.11.1",
    "python-dotenv>=1.2.2",
    "requests>=2.32.0",
]

[tool.uv]
environments = ["sys_platform == 'darwin'"]
